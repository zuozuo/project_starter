# ============================================
# ⚠️  安全警告 / SECURITY WARNING ⚠️
# ============================================
# 部署前必须修改以下配置为强密码：
#   - SECRET_KEY
#   - FIRST_SUPERUSER_PASSWORD
#   - POSTGRES_PASSWORD
#
# 生成安全密钥命令:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# 注意：生产环境禁止使用 "changethis" 作为任何密钥值
# ============================================

# Domain
# 部署时设置为生产域名，用于 Traefik 配置 TLS 证书
DOMAIN=localhost

# Frontend Host
# 后端用于生成邮件中的前端链接
FRONTEND_HOST=http://localhost:5173
# 生产环境示例: FRONTEND_HOST=https://dashboard.example.com

# Environment: local, staging, production
ENVIRONMENT=local

# Project Info
PROJECT_NAME="Your Project Name"
STACK_NAME=your-project-stack

# Backend
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173,https://localhost,https://localhost:5173"
SECRET_KEY=your-secret-key-here-change-in-production
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=your-strong-password-here

# Emails (可选 - 本地开发时可留空)
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=noreply@example.com
SMTP_TLS=True
SMTP_SSL=False
SMTP_PORT=587

# Postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-db-password-here

# Sentry (可选 - 错误监控)
SENTRY_DSN=

# Docker Images
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend

# ============================================
# AI/LLM 功能配置 (可选)
# ============================================
# 如需启用 AI 功能，请安装可选依赖: uv sync --extra ai
# OPENAI_API_KEY=your-openai-api-key
# ANTHROPIC_API_KEY=your-anthropic-api-key
# QDRANT_HOST=localhost
# QDRANT_PORT=6333
